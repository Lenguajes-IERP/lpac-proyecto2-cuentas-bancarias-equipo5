<UserControl x:Class="SalesPro.Wpf.Views.NuevaOrdenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             KeyboardNavigation.TabNavigation="Continue"
             AutomationProperties.Name="Registro de nueva orden de venta"
             AutomationProperties.HelpText="Pantalla maestro detalle para registrar productos en una orden de venta">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource LabelTextBrush}" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,5,0,12" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="12,7" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.1*" />
            <ColumnDefinition Width="24" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.ColumnSpan="3"
                   Text="Nueva orden de venta"
                   FontSize="28"
                   FontWeight="Bold"
                   Foreground="{DynamicResource HeadingTextBrush}"
                   Margin="0,0,0,20"
                   AutomationProperties.HeadingLevel="Level1" />

        <Border Grid.Row="1" Grid.Column="0" Padding="18" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Margin="0,0,0,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.4*" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,14,0">
                    <TextBlock Text="Cliente seleccionado" />
                    <TextBox Text="{Binding ClienteResumen, Mode=OneWay}"
                             IsReadOnly="True"
                             TabIndex="0"
                             ToolTip="Cliente asociado a la orden"
                             AutomationProperties.Name="Cliente seleccionado"
                             AutomationProperties.HelpText="Muestra el cliente asociado a la orden de venta" />
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,14,0">
                    <TextBlock Text="Fecha de orden" />
                    <TextBox Text="{Binding FechaOrden, Mode=OneWay}"
                             IsReadOnly="True"
                             TabIndex="1"
                             ToolTip="Fecha de registro de la orden"
                             AutomationProperties.Name="Fecha de orden"
                             AutomationProperties.HelpText="Muestra la fecha de registro de la orden de venta" />
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,14,0">
                    <TextBlock Text="Empleado Id" />
                    <TextBox Text="{Binding EmpleadoId, UpdateSourceTrigger=PropertyChanged}"
                             TabIndex="2"
                             ToolTip="Identificador del empleado que registra la orden"
                             AutomationProperties.Name="Identificador del empleado" />
                </StackPanel>

                <Button Grid.Column="3"
                        Content="Seleccionar Cliente"
                        Click="AbrirBusquedaCliente_Click"
                        VerticalAlignment="Center"
                        Background="{DynamicResource SecondaryButtonBrush}"
                        Foreground="{DynamicResource TextBrush}"
                        TabIndex="1"
                        ToolTip="Abrir ventana para buscar y seleccionar cliente"
                        AutomationProperties.Name="Seleccionar cliente"
                        AutomationProperties.HelpText="Abre la búsqueda de clientes para asociar uno a la orden" />

                <Button Grid.Column="4"
                        Content="Buscar y Agregar Producto..."
                        Click="AbrirBusqueda_Click"
                        VerticalAlignment="Center"
                        Background="{DynamicResource PrimaryBrush}"
                        Foreground="{DynamicResource PrimaryTextBrush}"
                        TabIndex="3"
                        ToolTip="Abrir ventana para buscar productos y agregarlos a la orden"
                        AutomationProperties.Name="Buscar y agregar producto"
                        AutomationProperties.HelpText="Abre la búsqueda de productos para seleccionar cantidad y agregar detalle a la orden" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" VerticalAlignment="Top" Margin="0,15,0,0">
            <TextBlock Text="{Binding Mensaje}"
                       Foreground="{DynamicResource MutedTextBrush}"
                       TextWrapping="Wrap"
                       AutomationProperties.LiveSetting="Polite"
                       AutomationProperties.Name="Mensaje de estado de la orden" />
        </StackPanel>

        <Border Grid.Row="1" Grid.Column="2" Grid.RowSpan="3" Padding="18" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom" Margin="0,18,0,0">
                    <Grid Margin="0,0,0,6">
                        <TextBlock Text="Subtotal" />
                        <TextBlock Text="{Binding Subtotal, StringFormat=₡{0:N2}}" HorizontalAlignment="Right" Foreground="{DynamicResource HeadingTextBrush}" AutomationProperties.Name="Subtotal de la orden" />
                    </Grid>
                    <Grid Margin="0,0,0,6">
                        <TextBlock Text="IVA estimado" />
                        <TextBlock Text="{Binding ImpuestoEstimado, StringFormat=₡{0:N2}}" HorizontalAlignment="Right" Foreground="{DynamicResource HeadingTextBrush}" AutomationProperties.Name="IVA estimado de la orden" />
                    </Grid>
                    <Separator Margin="0,8" />
                    <Grid Margin="0,0,0,18">
                        <TextBlock Text="Total estimado" FontWeight="Bold" />
                        <TextBlock Text="{Binding TotalEstimado, StringFormat=₡{0:N2}}" HorizontalAlignment="Right" Foreground="{DynamicResource HeadingTextBrush}" FontWeight="Bold" AutomationProperties.Name="Total estimado de la orden" />
                    </Grid>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="+" Command="{Binding IncrementarCommand}" Width="42" TabIndex="5" AutomationProperties.Name="Incrementar cantidad del producto seleccionado" />
                        <Button Content="-" Command="{Binding DecrementarCommand}" Width="42" TabIndex="6" AutomationProperties.Name="Disminuir cantidad del producto seleccionado" />
                        <Button Content="Remover" Command="{Binding RemoverProductoCommand}" TabIndex="7" AutomationProperties.Name="Remover producto seleccionado de la orden" />
                    </StackPanel>
                    <Button Content="Procesar orden" Command="{Binding ProcesarOrdenCommand}" Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,12,0,0" TabIndex="8" AutomationProperties.Name="Procesar orden de venta" />
                    <Button Content="Limpiar" Command="{Binding LimpiarCommand}" Margin="0,8,0,0" TabIndex="9" AutomationProperties.Name="Limpiar orden de venta" />
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Detalle de orden" FontSize="19" FontWeight="SemiBold" Foreground="{DynamicResource HeadingTextBrush}" Margin="0,0,0,12" AutomationProperties.HeadingLevel="Level2" />
                    <DataGrid AutoGenerateColumns="False"
                              ItemsSource="{Binding Detalles}"
                              SelectedItem="{Binding DetalleSeleccionado}"
                              IsReadOnly="True"
                              MinHeight="360"
                              Background="{DynamicResource CardBackgroundBrush}"
                              Foreground="{DynamicResource TextBrush}"
                              RowBackground="{DynamicResource DataGridRowBrush}"
                              AlternatingRowBackground="{DynamicResource DataGridAlternatingRowBrush}"
                              TabIndex="4"
                              AutomationProperties.Name="Tabla de detalles de la orden"
                              AutomationProperties.HelpText="Lista de productos agregados a la orden. Seleccione una fila para modificar cantidad o removerla">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Código" Binding="{Binding CodigoProducto}" Width="90" />
                            <DataGridTextColumn Header="Producto" Binding="{Binding NombreProducto}" Width="*" />
                            <DataGridTextColumn Header="Cant." Binding="{Binding Cantidad}" Width="55" />
                            <DataGridTextColumn Header="Precio" Binding="{Binding PrecioUnitario, StringFormat=₡{0:N2}}" Width="100" />
                            <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat=₡{0:N2}}" Width="110" />
                            <DataGridTextColumn Header="IVA" Binding="{Binding ImpuestoEstimado, StringFormat=₡{0:N2}}" Width="90" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
